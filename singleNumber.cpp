/*
lol this isn't even python you noob
*/

#include <stdio>
#include <stl>
using namespace std;

int singleNumber(vector<int>& nums) {
    int count = 0;
    map<string, int> m;
    for each num in nums
    {
        string a = atoi(num);
        m.insert<n>;
    }
    int small = m.find(m.count == 1);
    return small; 
}



int main()
{
}

